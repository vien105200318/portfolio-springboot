# Spring Boot Application Configuration
# 📚 SPRING BOOT: application.properties - Centralized Configuration

# === Server Configuration ===
server.port=${PORT:8080}
server.address=0.0.0.0

# === Thymeleaf Template Engine Configuration ===
# Cache templates trong production, disable trong development
# false = Thay đổi HTML sẽ reload ngay (không cần restart)
spring.thymeleaf.cache=${SPRING_THYMELEAF_CACHE:true}

# Prefix: Thư mục chứa templates
spring.thymeleaf.prefix=classpath:/templates/

# Suffix: Extension của template files
spring.thymeleaf.suffix=.html

# Encoding
spring.thymeleaf.encoding=UTF-8

# === DevTools Configuration ===
# Auto-restart khi code thay đổi
spring.devtools.restart.enabled=true

# LiveReload: Tự động refresh browser
spring.devtools.livereload.enabled=true

# === Logging Configuration ===
# Log level cho application
# TRACE < DEBUG < INFO < WARN < ERROR
logging.level.com.vien.portfolio=INFO
logging.level.org.springframework.web=INFO

# === Static Resources ===
# Các file trong /static sẽ serve trực tiếp
# Example: /static/css/style.css → http://localhost:8080/css/style.css
spring.web.resources.static-locations=classpath:/static/
