version: '3.8'

services:
  app:
    build: .
    container_name: portfolio-app
    ports:
      - "8080:8080"
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
